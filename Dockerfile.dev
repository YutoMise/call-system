# 開発用Dockerfile - 難読化なし
FROM node:22.15.0-alpine

WORKDIR /app

# package*.jsonをコピー
COPY package*.json ./

# 依存関係をインストール
RUN npm install --legacy-peer-deps

# curl をインストール (デバッグや疎通確認用)
RUN apk update && apk add curl

# プロジェクトの全ファイルをコピー（難読化なし）
COPY . .

# ポートを開放
EXPOSE 3002

# アプリケーションを実行
CMD ["npm", "start"]
